// Fill in the body of each function with the code required
// this is pretty easy. U need to return the operation of values.
// adds two arguments
var add = function(num1, num2) {
    return num1+num2;
};
// subtracts one fom another
var subtract = function(num1, num2) {
    return num1-num2;
};
// multiplies one by another
var multiply = function(num1, num2) {
    return num1*num2;
};
// divides one by another
var divide = function(num1, num2) {
    return num1/num2;
};
